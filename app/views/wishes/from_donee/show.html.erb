<div id="donee_wish_show">
  <h3><%= @wish.title %></h3>
  <p><%= @wish.description %></p>
  <%= render partial: "actions", locals: {wish: @wish} %>

  <div class="mdl-grid" id="wish_connections">
    <div class="donees md-cell" id="donees_connections">
       <h4><%= t("wish.donees.header") %></h4>
        <ul class="connection_list">
        <% for conn in (@wish.donee_connections) %>
          <li><%= conn.fullname %></li>
        <% end %>
        </ul>
    </div>
    
    <div class="mdl-layout-spacer"></div>
    
    <div class="donors md-cell" id="donors_connections">
      <h4><%= t("wish.donors.header") %></h4>
      <p><%= t("wish.from_donee.views.show.donors.count", total: @wish.donor_connections.count , owns: @wish.donor_connections.owned_by(current_user).count ) %>
        <ul class="connection_list">
        <% for conn in (@wish.donor_connections.owned_by(current_user)) %>
          <li><%= conn.fullname %></li>
        <% end %>
        </ul>
    </div>
  </div>
</div>  
